version: '3.8'

services:
  backend:
    build:
      context: ./apps/backend/YoutubeLiveApp
      dockerfile: Dockerfile
    container_name: devops-nuvem-backend
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
